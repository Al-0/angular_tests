import { PasswordActivationPipe } from './password-activation.pipe';

describe('PasswordActivationPipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordActivationPipe();
    expect(pipe).toBeTruthy();
  });
});
